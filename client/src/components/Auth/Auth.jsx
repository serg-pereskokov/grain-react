import React from "react"
import styles from './Auth.module.scss'

const Auth = props => {
    return (
        <div className={styles.Auth}>
            <h1>Auth</h1>
        </div>
    )
}

export { Auth }